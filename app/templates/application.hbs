Svgs can be embedded in a HTML payload and referenced from other places in the page, using the &lt;use&gt; element.<br>
<br>
If the &lt;svg&gt;&lt;use&gt; element is working then you should see an SVG of a house below this:<br><br>

<svg><use xlink:href="#lodge--camp"></use>
</svg>

{{outlet}}
